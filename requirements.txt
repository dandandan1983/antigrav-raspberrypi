
# Core DBus / BlueZ
pydbus==0.6.0
dbus-python==1.2.18

# PulseAudio control
pulsectl==23.5.0

# Bluetooth helpers

# Note: On Debian/Raspbian install `python3-bluez` via apt to provide system
# Bluetooth bindings. Building PyBluez via pip may fail on newer Python.
# If you need the PyPI package, build dependencies must be installed.

# GPIO
RPi.GPIO==0.7.1
gpiozero==1.6.2

# Testing
pytest==7.3.1
mock==5.0.1

# Audio processing (optional)
webrtcvad==2.0.10
numpy==1.25.0

